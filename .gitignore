sources/*/build/
sources/*/dl/
# ~90 MB per version, would bloat git history; rebuild with sources/obsidian/build.sh
repo/obsidian_*.deb
# ~130 MB per version, would bloat git history; rebuild with sources/mongodb-compass/build.sh
repo/mongodb-compass_*.deb
